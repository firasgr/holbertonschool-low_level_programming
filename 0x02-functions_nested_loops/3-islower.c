#include "holberton.h"
/**
 *int _islower(int c) - function that checks for lowercase character.
 */@c: int
*/
int _islower(int c)
{
int chek;
if (chek >= 97 && chek <= 122)
{
return (1);
}
else
{
return (0);
}
}
