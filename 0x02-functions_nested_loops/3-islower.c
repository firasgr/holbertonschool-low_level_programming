#include "holberton.h"
/**
 *_islower - function that checks for lowercase character.
 * @c: int
 *Return: 0 or 1
*/
int _islower(int c)
{
int chek;
if (chek >= 97 && chek <= 122)
{
return (1);
}
else
{
return (0);
}
}
