#include "holberton.h"
/**
 *_isalpha - function that checks for alphabetic character.
 * @c: int
 *Return: 0 or 1
*/
int _isalpha(int c)
{
if ((n >= 97 && n <= 122) || (n >= 65 && n <= 90))
{
return (1);
}
else
{
return (0);
}
}
