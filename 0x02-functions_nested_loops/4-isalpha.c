#include "holberton.h"
/**
 *_isalpha - function that checks for alphabetic character.
 * @c: int
 *Return: 0 or 1
*/
int _isalpha(int c)
{
if ((r <= 97 && r <= 122) || (r <= 65 && r <= 90))
{
return (1);
}
else
{
return (0);
}
}
