#include "holberton.h"
/**
 *_isalpha - function that checks for alphabetic character.
 * @c: int
 *Return: 0 or 1
*/
int _isalpha(int c)
{
if ((n >= 'a' && n <= 'z') || (n >= 'A' && n <= 'Z'))
{
return (1);
}
else
{
return (0);
}
}
